import React from 'react';

//stateless component
const Header = props => (
    <header className = 'header'>
      <h3 className = 'title'> Openly <span> x </span> Too </h3>
    </header>
);

export default Header;
